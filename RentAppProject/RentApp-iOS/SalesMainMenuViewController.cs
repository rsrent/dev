using Foundation;
using System;
using UIKit;
using RentAppProject;

namespace RentApp
{
    /*
    public partial class SalesMainMenuViewController : UIViewController
    {
        User Employee;

        public SalesMainMenuViewController (IntPtr handle) : base (handle)
        {
        }

		public void ParseInfo(User employee)
		{
			Employee = employee;
		}

        public override void ViewDidLoad()
        {
            base.ViewDidLoad();
            NavigationController.NavigationBar.Hidden = false;
			NavigationItem.SetLeftBarButtonItem(new UIBarButtonItem("Sign out", UIBarButtonItemStyle.Plain, (sender, e) =>
			{
                NavigationController.PopToRootViewController(true);
			}), true);

            NewCustomerButton.TouchUpInside += (sender, e) => {
                //new AddNewCustomer(this);
            };

            ExistingCustomersButton.TouchUpInside += (sender, e) => {
                //new ViewCustomers(this);
                //CustomersTableVC.Start(this);
            };
        }
    }
    */
}