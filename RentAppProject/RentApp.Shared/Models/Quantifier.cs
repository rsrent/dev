using System;
namespace RentAppProject
{
    public enum Quantifier
    {
        Day, Week, Month, Year

        //Once a week, every other week, once a month, every other month, every three months, every four months, twice a year, once a year
    }
}
