//enum ErrorState

import '../../../models.dart';
import '../../validators/validators.dart';

class TaskCreateUpdateErrors {
  void taskUpdated(Task task) {}

  bool isValid(bool isCreate) => true;
}
