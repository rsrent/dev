import 'package:flutter/material.dart';

Future<String> documentCreateDialog(BuildContext context) async {
  var _textFieldController = TextEditingController();
  var result = await showDialog<String>(
    context: context,
    builder: (context) {
      return AlertDialog(
        title: Text('Navn på dokumentet'),
        content: TextField(
          controller: _textFieldController,
          decoration: InputDecoration(hintText: "Dokumentets navn..."),
        ),
        actions: <Widget>[
          FlatButton(
            child: Text('TILBAGE'),
            onPressed: () {
              Navigator.of(context).pop(null);
            },
          ),
          FlatButton(
            child: Text('OPRET'),
            onPressed: () {
              Navigator.of(context).pop(_textFieldController.text);
            },
          ),
        ],
      );
    },
  );
  if (result is String)
    return result;
  else
    return null;
}
