//enum ErrorState

import '../../../models.dart';
import '../../validators/validators.dart';

class CustomerCreateUpdateErrors {
  void customerLoginUpdated(Customer customer) {}

  bool isValid(bool isCreate) => true;
}
