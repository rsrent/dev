//enum ErrorState

import '../../../models.dart';
import '../../validators/validators.dart';

class LocationCreateUpdateErrors {
  void locationLoginUpdated(Location location) {}

  bool isValid(bool isCreate) => true;
}
