using System;
using System.Linq.Expressions;

namespace Rent.Helpers
{
    public static class ExpressionHelpers
    {
        //public static dynamic CallWithArg<T>(this Expression<Func<T, dynamic>> expression, T arg) where T : class => arg != null ? expression.Compile()(arg) : null;
    }
}
